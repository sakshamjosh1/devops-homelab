Uncomplicated Firewall or UFW tells the machine what traffic or services to outbound and what to inbound
its like a security guard which allow who can come inside and who can go outside

we must not enable UFW before enabling SSH as if SSH is not enabled and UFW is active, we will never be able to have access to the machine

only port 22 is open as of now

ports are only gonna be opened when they are actually needed this is least principle privilege applied to networking



apiVersion: v1
kind: Service
metadata:
  name: url-shortener
  namespace: default
spec:
  type: NodePort
  selector:
    app: url-shortener
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30081
